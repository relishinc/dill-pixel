import { Padding, PointLike } from './types';
export declare function resolvePadding(paddingNum: number, size: number): number;
export declare function ensurePadding(padding: Partial<Padding> | PointLike): Padding;
