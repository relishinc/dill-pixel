{
  "name": "~NAME~",
  "version": "0.0.0",
  "private": true,
  "main": "index.html",
  "scripts": {
    "clean": "rimraf .assetpack .cache dist",
    "start": "~PACKAGE_MANAGER~ clean && dill-pixel start",
    "dev": "~PACKAGE_MANAGER~ clean && dill-pixel dev",
    "preview": "~PACKAGE_MANAGER~ clean && dill-pixel preview",
    "build": "~PACKAGE_MANAGER~ clean && dill-pixel build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dill-pixel": "latest"
  }
}
