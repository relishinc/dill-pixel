{
  "name": "~NAME~",
  "version": "0.0.0",
  "private": true,
  "main": "index.html",
  "scripts": {
    "clean": "rimraf .cache",
    "start": "~PACKAGE_MANAGER~ clean && vite",
    "dev": "~PACKAGE_MANAGER~ clean && vite",
    "preview": "~PACKAGE_MANAGER~ build && vite preview",
    "build": "~PACKAGE_MANAGER~ clean && vite build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dill-pixel": "latest"
  }
}
