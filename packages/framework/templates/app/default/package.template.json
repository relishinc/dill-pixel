{
  "name": "~NAME~",
  "version": "0.0.0",
  "private": true,
  "main": "index.html",
  "scripts": {
    "clean": "rimraf .assetpack .cache dist",
    "start": "dill-pixel start",
    "dev": "dill-pixel dev",
    "preview": "dill-pixel preview",
    "build": "dill-pixel build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dill-pixel": "latest"
  }
}
