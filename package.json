{
  "name": "dill-pixel-main",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "framework:build": "turbo build --filter={./packages/framework}",
    "framework:publish": "cd packages/framework && npm publish --tag next",
    "packages:update": "node ./scripts/update-package-versions.mjs",
    "packages:build": "sh ./scripts/build-packages.sh",
    "packages:publish": "node ./scripts/publish-packages.mjs packages/plugins/*,packages/storage-adapters/*",
    "build-publish-all": "pnpm framework:build && pnpm framework:publish && pnpm packages:update && pnpm packages:build && pnpm packages:publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "glob": "^10.4.1",
    "turbo": "^2.0.4"
  },
  "packageManager": "pnpm@9.3.0"
}
